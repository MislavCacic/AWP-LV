<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <style>
      .form-container {
        max-width: 600px;
        margin: 20px auto;
        padding: 20px;
        background-color: #f9f9f9;
        border-radius: 8px;
      }
      .form-group {
        margin-bottom: 15px;
      }
      label {
        display: block;
        margin-bottom: 5px;
        font-weight: bold;
      }
      textarea {
        width: 100%;
        padding: 8px;
        border: 1px solid #ddd;
        border-radius: 4px;
        box-sizing: border-box;
        resize: vertical;
        min-height: 150px;
      }
      .btn {
        display: inline-block;
        padding: 10px 20px;
        margin: 5px;
        text-decoration: none;
        color: white;
        border: none;
        border-radius: 4px;
        cursor: pointer;
      }
      .btn-primary {
        background-color: #ff9800;
      }
      .btn-secondary {
        background-color: #6c757d;
      }
      .btn:hover {
        opacity: 0.8;
      }
      .navbar {
        background-color: #333;
        overflow: hidden;
        padding: 10px 20px;
        margin-bottom: 20px;
      }
      .navbar a {
        float: left;
        color: white;
        text-decoration: none;
        padding: 8px 16px;
        margin-right: 10px;
      }
      .navbar a:hover {
        background-color: #ddd;
        color: black;
      }
      .navbar-right {
        float: right;
      }
      .user-info {
        color: white;
        float: right;
        padding: 8px 16px;
      }
      .project-info {
        background-color: #e7f3fe;
        border-left: 4px solid #2196F3;
        padding: 15px;
        margin-bottom: 20px;
      }
      .project-info h3 {
        margin-top: 0;
      }
    </style>
  </head>
  <body>
    <div class="navbar">
      <a href="/projects">Svi projekti</a>
      <% if (user) { %>
        <a href="/projects/my/managed">Moji projekti (voditelj)</a>
        <a href="/projects/my/member">Moji projekti (član)</a>
        <span class="user-info">Prijavljeni ste kao: <strong><%= user.username %></strong></span>
        <a href="/users/logout" class="navbar-right">Odjava</a>
      <% } else { %>
        <a href="/users/login" class="navbar-right">Prijava</a>
        <a href="/users/register" class="navbar-right">Registracija</a>
      <% } %>
    </div>
    
    <h1><%= title %></h1>
    
    <div class="project-info">
      <h3><%= project.naziv %></h3>
      <p><strong>Opis:</strong> <%= project.opis %></p>
      <p><strong>Datum početka:</strong> <%= project.datumPocetka %> | <strong>Datum završetka:</strong> <%= project.datumZavrsetka %></p>
    </div>
    
    <div class="form-container">
      <form action="/projects/<%= project.id %>/update-work" method="POST">
        <div class="form-group">
          <label for="obavljeniPoslovi">Obavljeni poslovi:</label>
          <textarea id="obavljeniPoslovi" name="obavljeniPoslovi" required><%= project.obavljeniPoslovi %></textarea>
          <small style="color: #666;">Ažurirajte informacije o poslovima koji su obavljeni na projektu.</small>
        </div>

        <button type="submit" class="btn btn-primary">Spremi promjene</button>
        <a href="/projects/my/member" class="btn btn-secondary">Odustani</a>
      </form>
    </div>
  </body>
</html>
